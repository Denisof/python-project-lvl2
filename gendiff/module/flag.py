"""Expose flags for build and format a deffirence."""

FLAG_CHANGED = 'changed'
FLAG_UNCHANGED = 'unchanged'
FLAG_ADDED = 'added'
FLAG_REMOVED = 'deleted'
FLAGS = (FLAG_CHANGED, FLAG_UNCHANGED, FLAG_ADDED, FLAG_REMOVED)
